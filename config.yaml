server:
  ui_port: 3000
  otlp_http_port: 4318
  grpc_port: 4317

database:
  path: "./litetrace.db"  # SQLite path; use "postgres://..." for Postgres

processor:
  batch_size: 500
  flush_interval_ms: 100
